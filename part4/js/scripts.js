/**
 * HBnB Web Client - Main Script
 * Loads all JavaScript modules and initializes page-specific functionality
 */

// Load all modules in order
// Note: In a production environment, you would use a module bundler
// For now, we'll load them sequentially via script tags in HTML

// This file serves as the main entry point
// All functions are loaded from separate modules:
// - utils.js: Configuration, cookies, API requests, helpers
// - auth.js: Authentication functions
// - places.js: Places loading and display
// - reviews.js: Reviews loading and submission

// Page-specific initialization happens in each HTML file's script tag
